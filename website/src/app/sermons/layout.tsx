export const metadata = {
  title: 'Sermons | Rehoboth Christian Church',
  description: 'Listen to sermons from Rehoboth Christian Church.',
};

export default function SermonsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
